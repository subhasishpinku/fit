class APIEndpoints {
  static const String baseUrl = "https://aipoweredfitness.com/api/";
  
  // Dashboard endpoint
  static const String dashboardDetailsAi = "${baseUrl}dashboard-details-api-ai?test=1";
  
  // Walking steps endpoint (adjust as needed)
  static const String walkingSteps = "${baseUrl}walking-steps-api"; // Update with actual endpoint
}